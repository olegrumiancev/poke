<% 
var alphabet = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-_'.split('')
  , length = 64
  , map = {}
  , seed = 0
  , i = 0
  , prev;

/**
 * Return a string representing the specified number.
 *
 * @param {Number} num The number to convert.
 * @returns {String} The string representation of the number.
 * @api public
 */
function encode(num) {
  var encoded = '';

  do {
    encoded = alphabet[num % length] + encoded;
    num = Math.floor(num / length);
  } while (num > 0);

  return encoded;
}

/**
 * Return the integer value specified by the given string.
 *
 * @param {String} str The string to convert.
 * @returns {Number} The integer value represented by the string.
 * @api public
 */
function decode(str) {
  var decoded = 0;

  for (i = 0; i < str.length; i++) {
    decoded = decoded * length + map[str.charAt(i)];
  }

  return decoded;
}

/**
 * Yeast: A tiny growing id generator.
 *
 * @returns {String} A unique id.
 * @api public
 */
function yeast() {
  var now = encode(+new Date());

  if (now !== prev) return seed = 0, prev = now;
  return now +'.'+ encode(seed++);
}                          %>
  
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Welcome to Poke | Redirecting…</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="color-scheme" content="dark light" />
  <meta name="theme-color" content="#ffabcc" />
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; connect-src 'self' https://poketube.fun; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; base-uri 'self'; form-action 'self'">
  <style>
    /* Custom Fonts */
    @font-face {
      src: url("/css/OpenDyslexic3-Regular.ttf");
      font-family: "OpenDyslexic3";
      font-style: normal;
      font-stretch: 1% 800%;
      font-display: swap;
    }

    @font-face {
      font-family: "PokeTube Flex";
      src: url("/static/robotoflex.ttf");
      font-style: normal;
      font-stretch: 1% 800%;
      font-display: swap;
    }

    :root { 
      --bg: #0b0b0b; 
      --surface: #141414; 
      --surface-border: #2a2a2a;
      --fg: #f3f3f3; 
      --muted: #a1a1a1; 
      --accent: #ffabcc; 
      --accent-hover: #ff8cb6;
      --danger: #ff6b6b;
    }

    * {
      box-sizing: border-box;
    }

    html, body { 
      margin: 0; padding: 0; height: 100%; 
      background: var(--bg); color: var(--fg); 
      font: 14px/1.4 system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Noto Sans", sans-serif; 
      overflow: hidden; /* Prevent global scrolling */
    }

    /* Titles */
    h1, h2 {
      font-family: "PokeTube Flex", sans-serif;
      font-stretch: ultra-expanded;
      font-weight: 1000;
      margin: 0px;
    }

    /* Split UI Layout */
    .split-layout {
      display: flex;
      height: 100vh;
      width: 100vw;
    }

    /* Left Side Branding */
    .branding-pane {
      flex: 1.2;
      /* Brighter pink gradient */
      background: linear-gradient(135deg, #a83258 0%, #ffabbc 80%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 40px;
      text-align: center;
      position: relative;
      border-right: 1px solid rgba(255, 171, 188, 0.2);
      overflow: hidden;
    }

    /* Background Text Container */
    .hype-container {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) rotate(-8deg);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 35px;
      z-index: 0;
      pointer-events: none;
      width: 150%;
      height: 90%; /* Leaves breathing room at top and bottom */
    }

    .hype-text {
      font-size: clamp(35px, 5vw, 75px);
      font-family: "PokeTube Flex", sans-serif;
      font-weight: 1000;
      color: #fff;
      white-space: nowrap;
      letter-spacing: 3px;
      text-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }
    
    .hype-text:nth-child(odd) { opacity: 0.12; margin-left: -5%; }
    .hype-text:nth-child(even) { opacity: 0.08; margin-left: 5%; }
    
    .branding-wrapper {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      width: max-content; /* Hugs the text to anchor mascot properly */
      z-index: 1;
    }
    
    
    .mascot {
      position: absolute;
      top: -119px;  
      right: -86px;  
      max-height: 290px;
      object-fit: contain;
      z-index: 2;
      filter: drop-shadow(0 15px 25px rgba(0,0,0,0.4));
      pointer-events: none;
    }

    .branding-content {
      position: relative;
      z-index: 1;
      background: transparent;
      padding: 0; 
      border: none;
      box-shadow: none;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .branding-content img.logo {
      max-width: 170px;
      margin-bottom: 12px;
      filter: drop-shadow(0 4px 12px rgba(0,0,0,0.3));
    }

    .branding-content h1 {
      font-size: clamp(40px, 7vw, 95px); /* Balanced Size */
      line-height: 1.05;
      letter-spacing: -0.02em;
      color: #fff;
      margin-bottom: 0;
      text-shadow: 0 8px 25px rgba(0,0,0,0.4);
    }

    /* Right Side Form */
    .form-pane {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      background: var(--bg);
      padding: 20px;
      overflow-y: auto;
    }

    .card { 
      width: 100%; 
      max-width: 400px; 
      background: transparent;
      border: none;
      box-shadow: none;
      padding: 0;
    }

    .card h2 { font-size: 26px; margin-bottom: 6px; color: var(--fg); }
    p { margin: 6px 0 0; color: var(--muted); }
    
    .row { display: flex; align-items: center; gap: 10px; margin-top: 12px; }
    .spinner { width: 16px; height: 16px; border-radius: 50%; border: 2px solid #333; border-top-color: var(--accent); animation: spin 0.8s linear infinite; }
    @keyframes spin { to { transform: rotate(360deg); } }

    .small { font-size: 12px; color: var(--muted); line-height: 1.4; }
    .hidden { display: none !important; }
    a { color: var(--accent); text-decoration: none; font-weight: 500; }
    a:hover { color: var(--accent-hover); text-decoration: underline; }

    /* Forms Inputs */
    .form-group { margin-top: 16px; }
    .form-group label { display: block; font-size: 13px; margin-bottom: 6px; color: var(--fg); font-weight: 500; }
    input[type="text"] { width: 100%; padding: 12px; border: 1px solid #333; border-radius: 8px; background: #0a0a0a; color: var(--fg); font-family: inherit; font-size: 14px; transition: border-color 0.2s; }
    input[type="text"]:focus { outline: none; border-color: var(--accent); }
    input[type="text"]::placeholder { color: #555; }

    /* Buttons */
    button.btn { width: 100%; padding: 12px; margin-top: 20px; border: none; border-radius: 8px; background: var(--accent); color: #000; font-weight: bold; cursor: pointer; font-family: inherit; font-size: 14px; transition: background 0.2s; }
    button.btn:hover { background: var(--accent-hover); }

    button.btn-text { background: transparent; color: var(--muted); border: none; font-size: 13px; cursor: pointer; padding: 6px; width: auto; margin: 12px auto 0; display: block; text-decoration: underline; transition: color 0.2s; }
    button.btn-text:hover { color: var(--fg); }

    /* Captcha UI */
    .captcha-container { background: #0a0a0a; border: 1px solid #333; border-radius: 8px; margin-bottom: 8px; display: flex; flex-direction: column; position: relative; }
    #captchaCanvas { width: 100%; height: 70px; display: block; border-top-left-radius: 8px; border-top-right-radius: 8px; }
    .captcha-toolbar { display: flex; justify-content: flex-end; gap: 6px; padding: 6px 10px; background: #111; border-top: 1px solid #222; border-bottom-left-radius: 8px; border-bottom-right-radius: 8px; }
    .btn-tool { background: none; border: none; color: var(--accent); font-size: 12px; cursor: pointer; display: flex; align-items: center; gap: 4px; padding: 4px 6px; border-radius: 4px; transition: background 0.2s; font-weight: 500; }
    .btn-tool:hover { background: rgba(255, 171, 204, 0.15); }

    /* A11y Popup Menu */
    .a11y-menu {
      position: absolute;
      bottom: 100%;
      right: 0;
      margin-bottom: 6px;
      background: #1a1a1a;
      border: 1px solid #333;
      border-radius: 8px;
      padding: 12px;
      width: 220px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.8);
      z-index: 10;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .a11y-menu.hidden { display: none; }
    .a11y-menu label { display: flex; flex-direction: column; gap: 4px; font-size: 12px; color: var(--fg); margin: 0 !important; }
    .a11y-menu input[type="range"] { width: 100%; accent-color: var(--accent); cursor: pointer; }

    /* Alerts Disclaimers */
    .error-msg { color: var(--danger); font-size: 13px; margin-top: 12px; display: none; padding: 10px; background: rgba(255, 107, 107, 0.08); border-radius: 8px; border: 1px solid rgba(255, 107, 107, 0.2); line-height: 1.4; }
    .disclaimer { margin-top: 24px; padding-top: 16px; border-top: 1px solid var(--surface-border); text-align: left; color: #777; font-size: 11px; line-height: 1.5; }

    /* Modal */
    .modal-overlay { position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0, 0, 0, 0.8); display: flex; align-items: center; justify-content: center; z-index: 1000; opacity: 0; pointer-events: none; transition: opacity 0.2s ease; padding: 20px; }
    .modal-overlay.active { opacity: 1; pointer-events: all; }
    .modal-content { background: var(--surface); border: 1px solid var(--surface-border); border-radius: 16px; width: 100%; max-width: 400px; padding: 24px; box-shadow: 0 20px 50px rgba(0,0,0,0.8); transform: translateY(10px); transition: transform 0.2s ease; }
    .modal-overlay.active .modal-content { transform: translateY(0); }
    .modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
    .close-modal { background: none; border: none; color: var(--muted); font-size: 24px; cursor: pointer; padding: 0; width: 32px; height: 32px; border-radius: 50%; }
    .close-modal:hover { color: var(--fg); background: #222; }

    /* Responsive */
    @media (max-width: 800px) {
      html, body { overflow-y: auto; }
      .split-layout { flex-direction: column; height: auto; min-height: 100vh; }
      .branding-pane { padding: 40px 20px 20px; border-right: none; border-bottom: 1px solid #2b111e; }
      .branding-wrapper { margin-top: 80px; }
      .mascot { max-height: 160px; top: -30px; right: -40px; }
      .form-pane { padding: 30px 20px; align-items: flex-start; }
    }
  </style>
</head>
<body>
  <noscript>
    <div class="split-layout">
      <div class="form-pane" style="width: 100%; justify-content: center;">
        <div class="card">
          <h2>JavaScript required</h2>
          <p>This page assigns you a user ID and redirects to your account.</p>
          <p class="small">Enable JavaScript and refresh, or go to <a href="/my-acc">/my-acc</a> and sign in manually.</p>
        </div>
      </div>
    </div>
  </noscript>

  <div class="split-layout" id="app">
    
    <div class="branding-pane">
      <div class="hype-container">
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
        <div class="hype-text">LIBERATE THE WEB</div>
      </div>
      <div class="branding-wrapper">
        <img src="/static/poke-chan-2.webp" alt="Poke-chan Mascot" class="mascot" />
        <div class="branding-content">
          <img src="/css/logo-poke.svg?v=5" alt="Poke Logo" class="logo" />
          <h1>Welcome<br>to Poke</h1>
        </div>
      </div>
    </div>

    <div class="form-pane">
      <div class="card">
        
        <div id="loading-state">
          <h2>Preparing account…</h2>
          <div class="row"><div class="spinner" aria-hidden="true"></div><p>Checking authorization securely.</p></div>
        </div>

        <div class="hidden" id="setup-state">
          <h2>Sign up</h2>
          <p>Do the steps below to jump in to the world of poke!</p>
          
          <div class="form-group">
            <label for="captchaInput">Enter the text shown below:</label>
            <div class="captcha-container">
              
              <div class="a11y-menu hidden" id="a11yMenu">
                <label>Speech Speed
                  <input type="range" id="ttsSpeed" min="0.3" max="1.5" step="0.1" value="0.8" aria-label="Adjust TTS Speed">
                </label>
                <label>Speech Pitch
                  <input type="range" id="ttsPitch" min="0" max="2" step="0.1" value="1.0" aria-label="Adjust TTS Pitch">
                </label>
              </div>

              <canvas id="captchaCanvas" width="300" height="70" role="img" aria-label="Captcha image containing distorted letters and numbers."></canvas>
              <div class="captcha-toolbar">
                <button type="button" class="btn-tool" onclick="toggleA11yMenu()" aria-label="Accessibility Settings">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>
                  Accessibility
                </button>
                <button type="button" class="btn-tool" onclick="playCaptchaAudio()" aria-label="Listen to captcha">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon><path d="M19.07 4.93a10 10 0 0 1 0 14.14M15.54 8.46a5 5 0 0 1 0 7.07"></path></svg>
                  Listen
                </button>
                <button type="button" class="btn-tool" onclick="generateCaptcha()" aria-label="Generate a new captcha image">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21.5 2v6h-6M21.34 15.57a10 10 0 1 1-.59-9.5l5.67 5.67"/></svg>
                  Refresh
                </button>
              </div>
            </div>
            
            <input type="text" id="captchaInput" autocomplete="off" placeholder="Type captcha here (case-sensitive)..." required aria-required="true" />
          </div>

          <div class="form-group">
            <label for="agreeInput">
              Click and read our <a href="https://poketube.fun/policies/" target="_blank" id="policies-link" onclick="markPoliciesRead()">Policies</a>.<br>Then type <strong>AGREE</strong> to accept, or <strong>DISAGREE</strong> to yk...disagree for some reason:
            </label>
            <input type="text" id="agreeInput" autocomplete="off" placeholder="AGREE or DISAGREE" required aria-required="true" />
          </div>

          <div class="error-msg" id="error-msg" role="alert"></div>

            <button class="btn" onclick="verifyAndProceed()">Jump in!</button>
          
          <button class="btn-text" onclick="openLoginModal()">I already have one....</button>

          <div class="disclaimer">
            You cannot use Poke for spamming or abuse. Poke accounts are for this single instance only and are not federated.<br><br>
            Poke is provided as is without warranty. <a href="/policies/privacy#warranty" target="_blank">Read Warranty Policy</a>.
          </div>
        </div>

      </div>
    </div>
  </div>

  <div class="modal-overlay" id="loginModal" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
    <div class="modal-content">
      <div class="modal-header">
        <h2 id="modalTitle">Log In</h2>
        <button class="close-modal" onclick="closeLoginModal()" aria-label="Close modal">&times;</button>
      </div>
      <p style="margin-top: 0; margin-bottom: 16px;">Enter your existing User ID to access ur account</p>
      <div class="form-group" style="margin-top: 0;">
        <label for="existingUserId">User ID:</label>
        <input type="text" id="existingUserId" placeholder="e.g. A1b2C3d4..." autocomplete="off" />
      </div>
      <button class="btn" onclick="loginExistingUser()">Log In</button>
    </div>
  </div>

  <script>
    var apiurl = "https://poketube.fun/api";
    <% var userid = yeast() %>
    
    var currentUserId = localStorage.getItem("UserID");
    var currentCaptchaText = "";
    var policiesRead = false;

    function markPoliciesRead() {
      policiesRead = true;
    }

    function toggleA11yMenu() {
      document.getElementById('a11yMenu').classList.toggle('hidden');
    }

    /* Close A11y menu if clicked outside */
    document.addEventListener('click', function(e) {
      const menu = document.getElementById('a11yMenu');
      if (!menu.classList.contains('hidden') && !e.target.closest('.captcha-container')) {
        menu.classList.add('hidden');
      }
    });

    function playCaptchaAudio() {
      if ('speechSynthesis' in window) {
        window.speechSynthesis.cancel();
        
        var speed = parseFloat(document.getElementById('ttsSpeed').value);
        var pitch = parseFloat(document.getElementById('ttsPitch').value);
        
        /* Convert characters to words, prefixing uppercase with Capital */
        var ttsChars = currentCaptchaText.split('').map(function(char) {
          if (char >= 'A' && char <= 'Z') {
            return 'Capital ' + char;
          }
          return char;
        });
        
        /* Join with a comma to force a small pause between characters in TTS */
        var spacedText = ttsChars.join(', ');
        
        var msg = new SpeechSynthesisUtterance("The captcha characters are: " + spacedText);
        msg.rate = speed; 
        msg.pitch = pitch;
        
        window.speechSynthesis.speak(msg);
      } else {
        alert("Text-to-speech is not supported in your browser.");
      }
    }

    function generateCaptcha() {
      const canvas = document.getElementById('captchaCanvas');
      const ctx = canvas.getContext('2d');
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      ctx.fillStyle = '#111';
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789';
      currentCaptchaText = '';
      for (let i = 0; i < 6; i++) {
        currentCaptchaText += chars.charAt(Math.floor(Math.random() * chars.length));
      }

      for (let i = 0; i < 15; i++) {
        ctx.strokeStyle = `rgba(255,255,255,0.4)`; /* Fixed syntax */
        ctx.lineWidth = Math.random() * 2;
        ctx.beginPath();
        ctx.moveTo(Math.random() * canvas.width, Math.random() * canvas.height);
        ctx.lineTo(Math.random() * canvas.width, Math.random() * canvas.height);
        ctx.stroke();
      }

      for (let i = 0; i < currentCaptchaText.length; i++) {
        ctx.save();
        const fontSize = 35 + Math.random() * 10;
        ctx.font = `bold ${fontSize}px "OpenDyslexic3", sans-serif`;
        
        ctx.translate(25 + (i * 45), 45 + (Math.random() * 10 - 5)); 
        ctx.rotate((Math.random() - 0.5) * 0.6);
        ctx.fillStyle = `hsl(${Math.random() * 360}, 70%, 75%)`; /* Fixed syntax */
        ctx.fillText(currentCaptchaText[i], 0, 0);
        ctx.restore();
      }

      for (let i = 0; i < 80; i++) {
        ctx.fillStyle = `rgba(255,255,255,0.2)`; /* Fixed syntax */
        ctx.beginPath();
        ctx.arc(Math.random() * canvas.width, Math.random() * canvas.height, Math.random() * 2, 0, 2 * Math.PI);
        ctx.fill();
      }
      
      document.getElementById('captchaInput').value = '';
      document.getElementById('captchaInput').focus();
    }

    if (currentUserId) {
      location.href = "/my-acc?ID=" + currentUserId + "&rparam=1";
    } else {
      document.getElementById('loading-state').classList.add('hidden');
      document.getElementById('setup-state').classList.remove('hidden');
      
      document.fonts.ready.then(function() {
        generateCaptcha();
      });
    }

    function verifyAndProceed() {
      var captchaInput = document.getElementById('captchaInput').value;
      var agreeInput = document.getElementById('agreeInput').value.trim().toUpperCase();
      var errorMsg = document.getElementById('error-msg');

      if (agreeInput === 'DISAGREE') {
        window.location.href = '/';
        return;
      }

      if (!policiesRead) {
        showError('You must click and read the Policies link before continuing.');
      } else if (captchaInput !== currentCaptchaText) {
        showError('Incorrect captcha. Please try again.');
        generateCaptcha(); 
      } else if (agreeInput !== 'AGREE') {
        showError('You must type AGREE to accept the policies, or DISAGREE to exit.');
      } else {
        errorMsg.style.display = 'none';
        
        localStorage.setItem('UserID', `<%- userid %>`);
        <% db.set(`user.${userid}`, userid) %>
        location.href = "/my-acc?ID=" + `<%- userid %>` + "&rparam=1";
      }
    }
    
    function showError(message) {
      var errorMsg = document.getElementById('error-msg');
      errorMsg.innerHTML = message;
      errorMsg.style.display = 'block';
    }

    const modal = document.getElementById('loginModal');
    
    function openLoginModal() {
      modal.classList.add('active');
      document.getElementById('existingUserId').focus();
    }
    
    function closeLoginModal() {
      modal.classList.remove('active');
    }
    
    modal.addEventListener('click', function(e) {
      if (e.target === modal) {
        closeLoginModal();
      }
    });

    function loginExistingUser() {
      var inputId = document.getElementById('existingUserId').value.trim();
      if (!inputId) {
        alert("Please enter a valid User ID.");
        return;
      }
      
      localStorage.setItem("UserID", inputId);
      location.href = "/my-acc?ID=" + inputId + "&rparam=1";
    }
  </script>
</body>
</html>